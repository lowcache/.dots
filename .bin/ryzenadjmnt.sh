!#/usr/bin/env bash

err() {
    local i=$1
    case "$i" in
      ''|*[0-1]*) printf "Invalid parameters. Try again."  && exit ;;
      ''|*[2-3]*) printf "Invaid ;;
      ''|*[4-5]*) ;;
    esac
    
}